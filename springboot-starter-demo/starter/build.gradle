bootJar.enabled = false
jar.enabled = true

dependencies {
    compile project(':starter-core')

    implementation 'org.springframework.boot:spring-boot-starter'
    implementation 'org.springframework.boot:spring-boot-configuration-processor'
}